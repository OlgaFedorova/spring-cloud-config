server:
  port: 8761

eureka:
  datacenter: datacenter1
  instance:
    hostname: peer1
  client:
    registerWithEureka: true
    fetchRegistry: true
    service-url:
      defaultZone: http://peer2:8762/eureka/

